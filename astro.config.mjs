// @ts-check
import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://your.site.here",
  output: "static",
  //   server: { host: true }, // uncomment if live server dosent work for some reason
});
